# Sanitized RC dogfood report

Status: owner-approved sanitized release evidence.

## Environment

- OpenClaw 2026.9.2
- Node.js 24.20.0 on Linux
- `openclaw-memory-migrator` 0.1.0-rc.1
- historical memory-lancedb-pro JSON exports
- current production slot: memory-core

The RC package was unpacked under `/tmp`. All generated artifacts stayed under
an isolated temporary run directory; no OpenClaw workspace, configuration,
plugin, service, or index was changed.

## Reconciliation

- source records: 1,533
- scoped counts: 927, 539, and 67
- largest scope: 927, below the verified 1,000-row export cap
- canonical records: 1,533
- missing source IDs: 0
- duplicate source IDs: 0
- rendered provenance markers: 1,533
- rendered files: 29

The source counts came from the original migration's scoped exports and
contemporaneous stats evidence. Source hashes and private paths are retained
outside this document.

## Safety results

- preflight passed for three explicit input files;
- default normalize and render commands produced no output;
- action/run-bound approvals were required for every write;
- canonical output and reports were mode `0600`;
- a second run from identical bytes produced byte-identical canonical and
  rendered artifacts;
- an attempted overwrite was rejected;
- Linux validation rejected a symbolic-link input and a pre-existing render
  directory;
- hardened rendering atomically published all 29 files and left no temporary
  render directory;
- all three real Pro envelopes identified version `1.0`, matched their declared
  counts, and contained explicit scopes;
- the strict adapter accepted the 1,533-record set and rejected a synthetic
  count-mismatched envelope on Linux;
- recursive secret/vector key scan found no leaked sensitive-named keys;
- production memory-core files and databases were not opened for writing.

The live plugin export already omitted vectors and did not contain
sensitive-named metadata, so the real-data run does not replace the synthetic
redaction fixture.

## Audit observations

The audit found 55 exact-text duplicate groups inherited from the source.
Approximate comparison was intentionally bounded to 1,000 of 1,174,211
candidate pairs, so this run does not claim a complete near-duplicate audit.

One secondary agent reported a dirty session source during the inventory.
Its memory source and vector index were healthy. No indexing action was taken
because runtime index repair was outside this transformation-only test.

## Outcome

The tested source path passed record reconciliation, determinism, provenance,
dry-run, approval, and no-overwrite gates. Public release still requires the
remaining checklist gates, including independent dry-run review, SBOM/signing,
and maintainer approval. Independent security review found no remaining
medium, high, or critical issues after output-ancestor TOCTOU hardening. A
deterministic injected-interruption test proves that a failed render remains
unpublished and can be restarted without duplicate amplification.
